using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NivelAcesso : MonoBehaviour
{
    [Header("Minhas Variáveis")]
    public int points;
    public float hit;
    [SerializeField] protected int hitKill;
    [SerializeField] private int lastKill;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
